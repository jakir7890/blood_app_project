import 'package:flutter/material.dart';


class BloodBank extends StatelessWidget {
  const BloodBank({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:  Center(
        child: Text("This is Blood Bank Screen"),
      ),
    );
  }
}
